
chunk_size: 128
save_unfull_chunk: true
local_cpu: true
max_local_cpu_size: 80.0
remote_url: "redis://127.0.0.1:6379"
remote_serde: "cachegen"

local_disk: null
max_local_disk_size: 0

save_decode_cache: false
cache_policy: "LRU"
numa_mode: null

zmq_event_publisher:
  enabled: true
  host: "0.0.0.0"
  port: 5557
  topic: "lmcache.events"
  # Must match the routable vLLM API endpoint registered in CacheRoute.
  instance_id: "127.0.0.1:8000"
  device: "cpu"
  cache_tier: "l1"